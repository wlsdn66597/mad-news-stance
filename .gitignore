# envs
.venv/
venv/
__pycache__/
*.pyc

# secrets
.env

# model cache / weights (원격에서 받고 Git 에는 절대 올리지 않음)
hf_cache/
models/

# large outputs (작은 지표 요약만 선택적으로 커밋)
results/*
!results/.gitkeep

# datasets (원격에 scp / 별도 다운로드)
data/*
!data/.gitkeep

# misc
*.log
.DS_Store
