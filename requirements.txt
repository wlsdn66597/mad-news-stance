# torch 는 CUDA 버전에 맞춰 따로 설치하세요 (README 참고). 예: CUDA 12.x
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers>=4.51.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
datasets>=2.19.0
sentencepiece>=0.2.0
pyyaml>=6.0
# (선택) 모델 다운로드 가속: 설치 후 HF_HUB_ENABLE_HF_TRANSFER=1 로 사용
hf_transfer>=0.1.6
